{
  "Urls": "http://+:8080",
  "RabbitMq": {
    "Host": "localhost",
    "_comment": "Credentials come from env vars (RABBITMQ_USER / RABBITMQ_PASS), not this file."
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  }
}
